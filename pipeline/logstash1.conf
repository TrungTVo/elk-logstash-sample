input {
  # Specify your input configuration here
  stdin {
    id => "trung-plugin-1"
  }
}

output {
  # Specify your output configuration here
  file {
    path => "/usr/share/logstash/workspace1_logs/elk-output.log"
  }
}